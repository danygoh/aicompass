[build]
builder = "NIXPACKS"
buildCommand = "cd aicompass-web && npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "cd aicompass-web && npx next start -p ${PORT:-3000} -H 0.0.0.0"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
