{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-cache && npx prisma generate && npm run build && cp -r public .next/standalone/public",
    "noCache": true
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
