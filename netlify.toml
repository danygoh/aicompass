[build]
  base = "aicompass-web"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
